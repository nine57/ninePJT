services:
  postgres:
    image: postgres:16-alpine
    env_file: .env
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    restart: unless-stopped

  backend:
    build: ./ninePjtBack
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data/media:/app/media
      - ./data/staticfiles:/app/staticfiles
    depends_on:
      - postgres
    restart: unless-stopped

  frontend:
    build:
      context: ./ninePjtFront
      args:
        VITE_API_BASE_URL: http://nine015.iptime.org:8000
    ports:
      - "80:80"
    restart: unless-stopped
